import { StyleSheet } from 'react-native';

const styles = () =>
  StyleSheet.create({
    container: {
      // backgroundColor: theme.colors.MODALBG,
      position: 'absolute',
    },
    spinnerTextStyle: {
      color: '#fff',
    },
  });

export default styles;
